---
title: "Update Contact"
openapi: "PATCH /api/v1/contacts/{id}"
description: "Update one or more fields of an existing contact. Only include fields you want to change."
---

<Note>This is a partial update (PATCH). Only include the fields you want to modify.</Note>

### Authorizations
<ParamField header="X-Api-Key" type="string" required>Your Agni API key.</ParamField>

### Path Parameters
<ParamField path="id" type="string" required>UUID of the contact to update.</ParamField>

### Body
<ParamField body="first_name" type="string">Updated first name.</ParamField>
<ParamField body="last_name" type="string">Updated last name.</ParamField>
<ParamField body="email" type="string">Updated email.</ParamField>
<ParamField body="phone" type="string">Updated phone (E.164).</ParamField>
<ParamField body="company" type="string">Updated company.</ParamField>
<ParamField body="metadata" type="object">Updated metadata.</ParamField>

### Response
<ResponseField name="success" type="boolean">Whether the request succeeded.</ResponseField>
<ResponseField name="message" type="string">Human-readable status message.</ResponseField>
<ResponseField name="data" type="object">The updated contact record.</ResponseField>

<RequestExample>

```bash cURL
curl -X PATCH https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b \
  -H "X-Api-Key: YOUR_API_KEY" -H "Content-Type: application/json" \
  -d '{"company": "Acme Corp Inc."}'
```

```python Python
import requests
response = requests.patch("https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b",
    headers={"X-Api-Key": "YOUR_API_KEY", "Content-Type": "application/json"}, json={"company": "Acme Corp Inc."})
print(response.json())
```

```javascript JavaScript
const response = await fetch("https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b", {
  method: "PATCH", headers: { "X-Api-Key": "YOUR_API_KEY", "Content-Type": "application/json" },
  body: JSON.stringify({ company: "Acme Corp Inc." })
});
console.log(await response.json());
```



```go Go
package main

import (
	"fmt"
	"net/http"
	"bytes"
	"encoding/json"
	"io"
)

func main() {
	jsonBody, _ := json.Marshal(map[string]interface{}{
		"company": "Acme Corp Inc.",
	})
	req, _ := http.NewRequest("PATCH", "https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b", bytes.NewBuffer(jsonBody))
	req.Header.Set("X-Api-Key", "YOUR_API_KEY")
	req.Header.Set("Content-Type", "application/json")
	client := &http.Client{}
	resp, _ := client.Do(req)
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```

```ruby Ruby
require "net/http"
require "json"
require "uri"

uri = URI("https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b")
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Patch.new(uri)
request["X-Api-Key"] = "YOUR_API_KEY"
request["Content-Type"] = "application/json"
request.body = {
  "company": "Acme Corp Inc."
}

response = http.request(request)
puts response.body
```

```php PHP
<?php

$ch = curl_init("https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_CUSTOMREQUEST, "PATCH");
curl_setopt($ch, CURLOPT_HTTPHEADER, ['X-Api-Key: YOUR_API_KEY', 'Content-Type: application/json']);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
	"company" => "Acme Corp Inc.",
]));

$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```java Java
import java.net.http.*;
import java.net.URI;

HttpClient client = HttpClient.newHttpClient();
String json = "{\"company\":\"Acme Corp Inc.\"}";
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b"))
    .method("PATCH", HttpRequest.BodyPublishers.ofString(json))
    .header("X-Api-Key", "YOUR_API_KEY")
    .header("Content-Type", "application/json")
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

```csharp C#
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("X-Api-Key", "YOUR_API_KEY");

var json = @"{
  ""company"": ""Acme Corp Inc.""
}";
var content = new StringContent(json, System.Text.Encoding.UTF8, "application/json");
var response = await client.PatchAsync("https://api.ravan.ai/api/v1/contacts/019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b", content);
var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

</RequestExample>

<ResponseExample>

```json 200
{
  "success": true, "message": "Contact updated successfully",
  "data": { "id": "019d3a2b-5c6d-7e8f-9a0b-1c2d3e4f5a6b", "first_name": "John", "last_name": "Smith", "email": "john@example.com", "phone": "+14155550100", "company": "Acme Corp Inc.", "updated_at": "2026-03-20T16:00:00Z" }
}
```




```json 400
{
  "code": 3,
  "message": "Invalid request parameters",
  "details": []
}
```




```json 401
{
  "code": 16,
  "message": "API key is missing or invalid",
  "details": []
}
```




```json 403
{
  "success": false,
  "message": "You do not have permission to perform this action",
  "error": {
    "code": "FORBIDDEN",
    "message": "Insufficient permissions for this resource"
  }
}
```




```json 500
{
  "success": false,
  "message": "Something went wrong on our end",
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "An unexpected error occurred. Please try again later."
  }
}
```




```json 502
{
  "success": false,
  "message": "Service temporarily unavailable",
  "error": {
    "code": "BAD_GATEWAY",
    "message": "Unable to reach upstream service. Please try again."
  }
}
```

</ResponseExample>
