---
title: "Discover URLs"
description: "Crawl a website and discover all available URLs before ingesting them. Use this to preview what pages will be indexed."
---

### Authorizations
<ParamField header="X-Api-Key" type="string" required>Your Agni API key.</ParamField>

### Body
<ParamField body="url" type="string" required>Root URL to start crawling from.</ParamField>
<ParamField body="max_depth" type="integer">Maximum link depth to follow. Default: `5`.</ParamField>
<ParamField body="limit" type="integer">Maximum number of URLs to return. Default: `100`.</ParamField>

### Response
<ResponseField name="success" type="boolean">Whether the request succeeded.</ResponseField>
<ResponseField name="message" type="string">Human-readable status message.</ResponseField>
<ResponseField name="data" type="object">Discovered URLs and total count.</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.ravan.ai/api/v1/rag/discover \
  -H "X-Api-Key: YOUR_API_KEY" -H "Content-Type: application/json" \
  -d '{"url": "https://docs.example.com", "max_depth": 3, "limit": 50}'
```

```python Python
import requests
response = requests.post("https://api.ravan.ai/api/v1/rag/discover",
    headers={"X-Api-Key": "YOUR_API_KEY"},
    json={"url": "https://docs.example.com", "max_depth": 3, "limit": 50})
print(response.json())
```

```javascript JavaScript
const response = await fetch("https://api.ravan.ai/api/v1/rag/discover", {
  method: "POST", headers: { "X-Api-Key": "YOUR_API_KEY", "Content-Type": "application/json" },
  body: JSON.stringify({ url: "https://docs.example.com", max_depth: 3, limit: 50 })
});
console.log(await response.json());
```



```go Go
package main

import (
	"fmt"
	"net/http"
	"bytes"
	"encoding/json"
	"io"
)

func main() {
	jsonBody, _ := json.Marshal(map[string]interface{}{
		"url": "https://docs.example.com",
		"max_depth": 3,
		"limit": 50,
	})
	req, _ := http.NewRequest("POST", "https://api.ravan.ai/api/v1/rag/discover", bytes.NewBuffer(jsonBody))
	req.Header.Set("X-Api-Key", "YOUR_API_KEY")
	req.Header.Set("Content-Type", "application/json")
	client := &http.Client{}
	resp, _ := client.Do(req)
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```

```ruby Ruby
require "net/http"
require "json"
require "uri"

uri = URI("https://api.ravan.ai/api/v1/rag/discover")
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Post.new(uri)
request["X-Api-Key"] = "YOUR_API_KEY"
request["Content-Type"] = "application/json"
request.body = {
  "url": "https://docs.example.com",
  "max_depth": 3,
  "limit": 50
}

response = http.request(request)
puts response.body
```

```php PHP
<?php

$ch = curl_init("https://api.ravan.ai/api/v1/rag/discover");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_CUSTOMREQUEST, "POST");
curl_setopt($ch, CURLOPT_HTTPHEADER, ['X-Api-Key: YOUR_API_KEY', 'Content-Type: application/json']);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
	"url" => "https://docs.example.com",
	"max_depth" => 3,
	"limit" => 50,
]));

$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```java Java
import java.net.http.*;
import java.net.URI;

HttpClient client = HttpClient.newHttpClient();
String json = "{\"url\":\"https://docs.example.com\",\"max_depth\":3,\"limit\":50}";
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.ravan.ai/api/v1/rag/discover"))
    .method("POST", HttpRequest.BodyPublishers.ofString(json))
    .header("X-Api-Key", "YOUR_API_KEY")
    .header("Content-Type", "application/json")
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

```csharp C#
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("X-Api-Key", "YOUR_API_KEY");

var json = @"{
  ""url"": ""https://docs.example.com"",
  ""max_depth"": 3,
  ""limit"": 50
}";
var content = new StringContent(json, System.Text.Encoding.UTF8, "application/json");
var response = await client.PostAsync("https://api.ravan.ai/api/v1/rag/discover", content);
var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

</RequestExample>

<ResponseExample>

```json 200
{
  "success": true,
  "message": "Discovery completed",
  "data": {
    "urls": [
      "https://docs.example.com/",
      "https://docs.example.com/getting-started",
      "https://docs.example.com/api-reference",
      "https://docs.example.com/pricing"
    ],
    "total": 4
  }
}
```




```json 400
{
  "code": 3,
  "message": "Invalid request parameters",
  "details": []
}
```




```json 401
{
  "code": 16,
  "message": "API key is missing or invalid",
  "details": []
}
```




```json 403
{
  "success": false,
  "message": "You do not have permission to perform this action",
  "error": {
    "code": "FORBIDDEN",
    "message": "Insufficient permissions for this resource"
  }
}
```




```json 500
{
  "success": false,
  "message": "Something went wrong on our end",
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "An unexpected error occurred. Please try again later."
  }
}
```




```json 502
{
  "success": false,
  "message": "Service temporarily unavailable",
  "error": {
    "code": "BAD_GATEWAY",
    "message": "Unable to reach upstream service. Please try again."
  }
}
```

</ResponseExample>
