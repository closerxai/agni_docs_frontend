---
title: "Disconnect Cal.com"
openapi: "POST /api/v1/calcom/disconnect"
description: "Disconnect the Cal.com integration from your organization. This revokes OAuth tokens and removes the scheduling connection."
---

<Warning>
  This will disconnect Cal.com from your organization. You will need to re-authorize via OAuth to reconnect.
</Warning>

### Authorizations

<ParamField header="X-Api-Key" type="string" required>
  Your Agni API key. Find it in your [Ravan.ai Dashboard](https://app.ravan.ai) under **Settings → API Keys**.
</ParamField>

---

### Response

<ResponseField name="success" type="boolean">Whether the request succeeded.</ResponseField>
<ResponseField name="message" type="string">Human-readable status message.</ResponseField>
<ResponseField name="data" type="object">Empty object on success.</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.ravan.ai/api/v1/calcom/disconnect \
  -H "X-Api-Key: YOUR_API_KEY"
```

```python Python
import requests

response = requests.post(
    "https://api.ravan.ai/api/v1/calcom/disconnect",
    headers={"X-Api-Key": "YOUR_API_KEY"}
)
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://api.ravan.ai/api/v1/calcom/disconnect",
  {
    method: "POST",
    headers: { "X-Api-Key": "YOUR_API_KEY" }
  }
);
const data = await response.json();
console.log(data);
```

</RequestExample>

<ResponseExample>

```json 200
{
  "success": true,
  "message": "Cal.com disconnected successfully",
  "data": {}
}
```

</ResponseExample>
