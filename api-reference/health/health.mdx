---
title: "Health Check"
description: "Check whether the Agni API service is running and responsive. No authentication required."
---

<Note>
  This endpoint does not require authentication. Use it to verify the service is reachable before making authenticated requests.
</Note>

### Response

<ResponseField name="status" type="string">
  Service health status. Returns `"healthy"` when the service is operational.
</ResponseField>

<ResponseField name="service" type="string">
  Name of the service instance responding (e.g. `"integration_service"`).
</ResponseField>

<ResponseField name="time" type="string">
  Current server time in ISO 8601 UTC format.
</ResponseField>

<RequestExample>

```bash cURL
curl https://api.ravan.ai/health
```

```python Python
import requests

response = requests.get("https://api.ravan.ai/health")
print(response.json())
```

```javascript JavaScript
const response = await fetch("https://api.ravan.ai/health");
const data = await response.json();
console.log(data);
```



```go Go
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {
	req, _ := http.NewRequest("GET", "https://api.ravan.ai/health", nil)
	client := &http.Client{}
	resp, _ := client.Do(req)
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```

```ruby Ruby
require "net/http"
require "json"
require "uri"

uri = URI("https://api.ravan.ai/health")
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Get.new(uri)

response = http.request(request)
puts response.body
```

```php PHP
<?php

$ch = curl_init("https://api.ravan.ai/health");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```java Java
import java.net.http.*;
import java.net.URI;

HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.ravan.ai/health"))
    .method("GET", HttpRequest.BodyPublishers.noBody())
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

```csharp C#
using var client = new HttpClient();

var response = await client.GetAsync("https://api.ravan.ai/health");
var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

</RequestExample>

<ResponseExample>

```json 200
{
  "status": "healthy",
  "service": "integration_service",
  "time": "2026-03-20T14:30:00Z"
}
```




```json 500
{
  "success": false,
  "message": "Something went wrong on our end",
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "An unexpected error occurred. Please try again later."
  }
}
```




```json 502
{
  "success": false,
  "message": "Service temporarily unavailable",
  "error": {
    "code": "BAD_GATEWAY",
    "message": "Unable to reach upstream service. Please try again."
  }
}
```

</ResponseExample>
