---
title: "GHL OAuth Authorize"
openapi: "GET /api/v1/ghl/oauth/authorize"
description: "Initiate the GoHighLevel OAuth authorization flow. This endpoint redirects the browser to GHL's consent page — it does not return a JSON response."
---

<Note>
  This is a browser redirect endpoint. Open this URL in a browser to start the GHL OAuth flow. It does not return a JSON response.
</Note>

### Authorizations

<ParamField header="X-Api-Key" type="string" required>
  Your Agni API key. Find it in your [Ravan.ai Dashboard](https://app.ravan.ai) under **Settings → API Keys**.
</ParamField>

---

### Query Parameters

<ParamField query="organization_id" type="string" required>
  Your organization ID. The GHL connection will be linked to this organization.
</ParamField>

---

This endpoint redirects the browser to GoHighLevel's OAuth consent page. After the user authorizes access, GHL redirects back to the [OAuth Callback](/api-reference/ghl/oauth-callback) endpoint to complete the connection.

<RequestExample>

```bash cURL
# Open in browser — this is a redirect, not a JSON API call
curl -v "https://api.ravan.ai/api/v1/ghl/oauth/authorize?organization_id=1268c1f0-19f3-47db-aefb-c16a7c3ace6e" \
  -H "X-Api-Key: YOUR_API_KEY"
```



```go Go
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {
	req, _ := http.NewRequest("GET", "https://api.ravan.ai/api/v1/ghl/oauth/authorize?organization_id=1268c1f0-19f3-47db-aefb-c16a7c3ace6e", nil)
	req.Header.Set("X-Api-Key", "YOUR_API_KEY")
	client := &http.Client{}
	resp, _ := client.Do(req)
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```

```ruby Ruby
require "net/http"
require "json"
require "uri"

uri = URI("https://api.ravan.ai/api/v1/ghl/oauth/authorize?organization_id=1268c1f0-19f3-47db-aefb-c16a7c3ace6e")
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Get.new(uri)
request["X-Api-Key"] = "YOUR_API_KEY"

response = http.request(request)
puts response.body
```

```php PHP
<?php

$ch = curl_init("https://api.ravan.ai/api/v1/ghl/oauth/authorize?organization_id=1268c1f0-19f3-47db-aefb-c16a7c3ace6e");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, ['X-Api-Key: YOUR_API_KEY']);

$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```java Java
import java.net.http.*;
import java.net.URI;

HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.ravan.ai/api/v1/ghl/oauth/authorize?organization_id=1268c1f0-19f3-47db-aefb-c16a7c3ace6e"))
    .method("GET", HttpRequest.BodyPublishers.noBody())
    .header("X-Api-Key", "YOUR_API_KEY")
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

```csharp C#
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("X-Api-Key", "YOUR_API_KEY");

var response = await client.GetAsync("https://api.ravan.ai/api/v1/ghl/oauth/authorize?organization_id=1268c1f0-19f3-47db-aefb-c16a7c3ace6e");
var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

</RequestExample>

<ResponseExample>

```json 400
{
  "code": 3,
  "message": "Invalid request parameters",
  "details": []
}
```




```json 401
{
  "code": 16,
  "message": "API key is missing or invalid",
  "details": []
}
```




```json 403
{
  "success": false,
  "message": "You do not have permission to perform this action",
  "error": {
    "code": "FORBIDDEN",
    "message": "Insufficient permissions for this resource"
  }
}
```




```json 500
{
  "success": false,
  "message": "Something went wrong on our end",
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "An unexpected error occurred. Please try again later."
  }
}
```




```json 502
{
  "success": false,
  "message": "Service temporarily unavailable",
  "error": {
    "code": "BAD_GATEWAY",
    "message": "Unable to reach upstream service. Please try again."
  }
}
```

</ResponseExample>
