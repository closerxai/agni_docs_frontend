---
title: "Get GHL Agent by Calendar"
openapi: "GET /api/v1/ghl/setup/agents/calendar/{calendar_id}"
description: "Look up which agent is assigned to a specific GHL calendar. Returns the agent details and assignment status."
---

### Authorizations

<ParamField header="X-Api-Key" type="string" required>
  Your Agni API key. Find it in your [Ravan.ai Dashboard](https://app.ravan.ai) under **Settings → API Keys**.
</ParamField>

---

### Path Parameters

<ParamField path="calendar_id" type="string" required>
  The GHL calendar ID to look up.
</ParamField>

---

### Response

<ResponseField name="success" type="boolean">Whether the request succeeded.</ResponseField>
<ResponseField name="message" type="string">Human-readable status message.</ResponseField>
<ResponseField name="data" type="object">
  Agent-calendar assignment details.

  <Expandable title="data">
    <ResponseField name="agent_id" type="string">UUID of the agent assigned to this calendar.</ResponseField>
    <ResponseField name="agent_name" type="string">Display name of the assigned agent.</ResponseField>
    <ResponseField name="calendar_id" type="string">GHL calendar ID.</ResponseField>
    <ResponseField name="is_active" type="boolean">Whether the agent-calendar assignment is active.</ResponseField>
    <ResponseField name="created_at" type="string">ISO 8601 timestamp of when the assignment was created.</ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl "https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/cal_abc123def456" \
  -H "X-Api-Key: YOUR_API_KEY"
```

```python Python
import requests

calendar_id = "cal_abc123def456"
response = requests.get(
    f"https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/{calendar_id}",
    headers={"X-Api-Key": "YOUR_API_KEY"}
)
print(response.json())
```

```javascript JavaScript
const calendarId = "cal_abc123def456";
const response = await fetch(
  `https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/${calendarId}`,
  { headers: { "X-Api-Key": "YOUR_API_KEY" } }
);
const data = await response.json();
console.log(data);
```



```go Go
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {
	req, _ := http.NewRequest("GET", "https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/cal_abc123def456", nil)
	req.Header.Set("X-Api-Key", "YOUR_API_KEY")
	client := &http.Client{}
	resp, _ := client.Do(req)
	defer resp.Body.Close()
	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```

```ruby Ruby
require "net/http"
require "json"
require "uri"

uri = URI("https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/cal_abc123def456")
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Get.new(uri)
request["X-Api-Key"] = "YOUR_API_KEY"

response = http.request(request)
puts response.body
```

```php PHP
<?php

$ch = curl_init("https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/cal_abc123def456");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_HTTPHEADER, ['X-Api-Key: YOUR_API_KEY']);

$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```java Java
import java.net.http.*;
import java.net.URI;

HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/cal_abc123def456"))
    .method("GET", HttpRequest.BodyPublishers.noBody())
    .header("X-Api-Key", "YOUR_API_KEY")
    .build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

```csharp C#
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("X-Api-Key", "YOUR_API_KEY");

var response = await client.GetAsync("https://api.ravan.ai/api/v1/ghl/setup/agents/calendar/cal_abc123def456");
var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

</RequestExample>

<ResponseExample>

```json 200
{
  "success": true,
  "message": "Agent fetched successfully",
  "data": {
    "agent_id": "019d1fdb-98e9-7c87-86c7-321e2fa98733",
    "agent_name": "Ava",
    "calendar_id": "cal_abc123def456",
    "is_active": true,
    "created_at": "2026-03-15T09:00:00Z"
  }
}
```




```json 400
{
  "code": 3,
  "message": "Invalid request parameters",
  "details": []
}
```




```json 401
{
  "code": 16,
  "message": "API key is missing or invalid",
  "details": []
}
```




```json 403
{
  "success": false,
  "message": "You do not have permission to perform this action",
  "error": {
    "code": "FORBIDDEN",
    "message": "Insufficient permissions for this resource"
  }
}
```




```json 500
{
  "success": false,
  "message": "Something went wrong on our end",
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "An unexpected error occurred. Please try again later."
  }
}
```




```json 502
{
  "success": false,
  "message": "Service temporarily unavailable",
  "error": {
    "code": "BAD_GATEWAY",
    "message": "Unable to reach upstream service. Please try again."
  }
}
```

</ResponseExample>
